services:
  - type: web
    name: vitrinefc
    runtime: node
    plan: free
    rootDir: app
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: DB_GITHUB_TOKEN
        sync: false
      - key: DB_GITHUB_REPO
        sync: false
